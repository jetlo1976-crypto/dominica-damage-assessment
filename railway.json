{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "apt-get update && apt-get install -y libexpat1 libgdal-dev gdal-bin python3-gdal && ldconfig"
  },
  "deploy": {
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}