{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "apt-get update && apt-get install -y gdal-bin libgdal-dev python3-gdal libexpat1 libspatialindex-dev"
  },
  "deploy": {
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}